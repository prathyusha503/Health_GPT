# No API key required — this project runs 100% free and locally.
# CLIP vision model runs on your CPU via HuggingFace transformers.
# Translation uses Google Translate (free, no key).
# Audio uses Google TTS (free, no key).
